extends layout

block content
  .container(ng-controller="main.js")
    .card.card-container
      a(ng-href='/welcome')
        img(ng-src='/img/bunny.png', alt='logo', width='90', height='90')
      p#profile-name.profile-name-card #{title}
      .savedView
        h2 Thank you for joining!
        p
        h4
          | #{message}
      a.btn.btn-info(ng-href='/', role='button') LOG IN
      br